pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
    }
    includeBuild("tests/tck-build-logic")
}

rootProject.name = "reachability-metadata"
